---
/**
 * Deliberately carries no version masthead. This is a procedural "how to get your data erased"
 * page, not a versioned disclosure — it restates rights the privacy policy already grants and
 * creates no new obligation, so it must never appear in `legalDocuments` and must never gate an
 * in-app acceptance prompt. Google Play links to it from the store listing.
 */
import OverLitDataDeletionBody from "../../content/legal/overlit-data-deletion.md";
---

<OverLitDataDeletionBody />
